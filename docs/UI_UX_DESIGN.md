# OpenCode-on-IM UI/UX 设计规格

> **版本**: v1.0  
> **日期**: 2026-01-13

---

## 目录

1. [命令流程图](#1-命令流程图)
2. [消息卡片设计](#2-消息卡片设计)
3. [交互状态机](#3-交互状态机)
4. [错误处理](#4-错误处理)
5. [多语言支持](#5-多语言支持)

---

## 1. 命令流程图

### 1.1 首次绑定流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           首次绑定流程                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   用户                          Bot                         OpenCode        │
│    │                             │                              │           │
│    │  ───────── /start ────────► │                              │           │
│    │                             │                              │           │
│    │  ◄─── "请扫描二维码绑定" ─── │                              │           │
│    │                             │                              │           │
│    │  ─── 发送二维码数据 ───────► │                              │           │
│    │      (eyJ...base64)         │                              │           │
│    │                             │                              │           │
│    │                             │ ──── 验证签名 ────►          │           │
│    │                             │ ◄─── 验证成功 ─────          │           │
│    │                             │                              │           │
│    │                             │ ──── 创建绑定 ────►          │           │
│    │                             │ ◄─── 绑定完成 ─────          │           │
│    │                             │                              │           │
│    │  ◄─── "绑定成功！实例:xxx" ─ │                              │           │
│    │       [卡片: 查看状态]       │                              │           │
│    │                             │                              │           │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 发送消息流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           消息处理流程                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   用户                          Bot                         OpenCode        │
│    │                             │                              │           │
│    │  ─── "帮我写个函数" ───────► │                              │           │
│    │                             │                              │           │
│    │                             │ ──── 发送消息 ────►          │           │
│    │                             │                              │           │
│    │  ◄─── "⏳ 处理中..." ────── │                              │           │
│    │                             │                              │           │
│    │                             │ ◄─── 事件流 ────────         │           │
│    │                             │      (多个事件)              │           │
│    │                             │                              │           │
│    │  ◄─── 实时输出 (多条) ───── │                              │           │
│    │       [文本/图片]           │                              │           │
│    │                             │                              │           │
│    │  ◄─── "✅ 完成" ─────────── │                              │           │
│    │       📡 在线用户: @bob      │                              │           │
│    │                             │                              │           │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.3 多实例切换流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           实例切换流程                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   用户                          Bot                                         │
│    │                             │                                          │
│    │  ─── /list ────────────────► │                                         │
│    │                             │                                          │
│    │  ◄─── 已绑定实例:           │                                          │
│    │       • api-server (活跃)   │                                          │
│    │       • web-frontend        │                                          │
│    │       • data-pipeline       │                                          │
│    │                             │                                          │
│    │  ─── /switch web-frontend ─► │                                         │
│    │                             │                                          │
│    │  ◄─── ✅ 已切换到:          │                                          │
│    │       web-frontend          │                                          │
│    │       📡 在线: @alice        │                                          │
│    │                             │                                          │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 消息卡片设计

### 2.1 Telegram 消息格式

#### 欢迎消息
```
┌────────────────────────────────────┐
│ 🎉 绑定成功！                       │
│                                    │
│ 实例: `api-server`                 │
│ ID: `a1b2c3d4`                     │
│                                    │
│ 📡 在线用户: 只有你                 │
│                                    │
│ ─────────────────────────────────  │
│ 💡 发送任何消息开始与 AI 对话       │
│ 使用 /help 查看命令列表            │
│                                    │
│ [📊 查看状态]  [🌐 打开终端]        │
└────────────────────────────────────┘
```

#### 状态消息
```
┌────────────────────────────────────┐
│ 📦 实例状态                         │
│                                    │
│ ▸ api-server (当前)                │
│   └─ 会话: 3 个活跃                │
│   └─ 状态: 🟢 在线                  │
│                                    │
│ ▸ web-frontend                     │
│   └─ 会话: 1 个活跃                │
│   └─ 状态: 🟢 在线                  │
│                                    │
│ ─────────────────────────────────  │
│ 📡 其他在线用户: @bob, @charlie    │
└────────────────────────────────────┘
```

#### AI 回复 (短文本)
```
┌────────────────────────────────────┐
│ 🤖 [api-server]                    │
│                                    │
│ 这是一个用于计算阶乘的函数:        │
│                                    │
│ ```python                          │
│ def factorial(n):                  │
│     if n <= 1:                     │
│         return 1                   │
│     return n * factorial(n - 1)    │
│ ```                                │
│                                    │
│ ─────────────────────────────────  │
│ 📡 在线: @alice                    │
└────────────────────────────────────┘
```

#### AI 回复 (长代码 → 图片)
```
┌────────────────────────────────────┐
│ 🤖 [api-server]                    │
│                                    │
│ 生成了一个完整的 API 模块:         │
│                                    │
│ ┌──────────────────────────────┐  │
│ │  [代码截图 PNG]               │  │
│ │                              │  │
│ │  1 │ from fastapi import...  │  │
│ │  2 │ from pydantic import... │  │
│ │  3 │                         │  │
│ │  ...                         │  │
│ │ 47 │ if __name__ == "..."    │  │
│ │                              │  │
│ └──────────────────────────────┘  │
│                                    │
│ [📋 复制代码]  [🌐 在终端查看]      │
│                                    │
│ ─────────────────────────────────  │
│ 📡 在线: @alice, @bob             │
└────────────────────────────────────┘
```

#### Web Terminal 卡片
```
┌────────────────────────────────────┐
│ 🌐 Web Terminal                    │
│                                    │
│ 类型: ttyd                         │
│ 端口: 7681                         │
│                                    │
│ ⚠️ 确保你的设备可以访问服务器       │
│                                    │
│ [🔗 打开终端 →]                    │
│ http://your-server:7681            │
└────────────────────────────────────┘
```

#### 实例离线告警
```
┌────────────────────────────────────┐
│ ⚠️ 实例离线                        │
│                                    │
│ 实例 `api-server` 已断开连接       │
│ 时间: 2026-01-13 14:32:00         │
│                                    │
│ 可能原因:                          │
│ • OpenCode 进程已停止              │
│ • 网络连接中断                     │
│ • 服务器重启中                     │
│                                    │
│ [🔄 重试连接]  [📊 查看日志]       │
└────────────────────────────────────┘
```

### 2.2 钉钉消息卡片

#### ActionCard 格式
```json
{
  "msgtype": "actionCard",
  "actionCard": {
    "title": "🤖 AI 代码回复",
    "text": "### api-server\n\n生成了一个完整的 API 模块\n\n![代码截图](https://...)\n\n---\n📡 在线: @alice, @bob",
    "btnOrientation": "1",
    "btns": [
      {
        "title": "📋 复制代码",
        "actionURL": "dingtalk://..."
      },
      {
        "title": "🌐 打开终端",
        "actionURL": "https://your-server:7681"
      }
    ]
  }
}
```

#### Markdown 消息 (简单回复)
```json
{
  "msgtype": "markdown",
  "markdown": {
    "title": "AI 回复",
    "text": "### 🤖 api-server\n\n这是一个用于计算阶乘的函数。\n\n[查看代码](https://...)\n\n---\n📡 在线: @alice"
  }
}
```

---

## 3. 交互状态机

### 3.1 用户状态

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           用户状态机                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│                            ┌─────────┐                                      │
│                   /start   │ UNBOUND │                                      │
│              ┌────────────►│ (未绑定) │◄────────────┐                       │
│              │             └────┬────┘             │                        │
│              │                  │                  │                        │
│              │                  │ 扫码成功         │ /unbind (全部)         │
│              │                  ▼                  │                        │
│              │             ┌─────────┐             │                        │
│              │             │  BOUND  │─────────────┘                        │
│              │      ┌─────►│ (已绑定) │◄─────┐                              │
│              │      │      └────┬────┘      │                              │
│              │      │           │           │                              │
│              │      │ /switch   │ 发消息    │ 消息完成                      │
│              │      │           ▼           │                              │
│              │      │      ┌─────────┐      │                              │
│              │      └──────│ ACTIVE  │──────┘                              │
│              │             │ (活跃中) │                                     │
│              │             └────┬────┘                                     │
│              │                  │                                          │
│              │                  │ 实例离线                                  │
│              │                  ▼                                          │
│              │             ┌─────────┐                                     │
│              └─────────────│OFFLINE  │                                     │
│                 重连失败    │ (离线)  │                                     │
│                            └─────────┘                                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 实例状态

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           实例状态机                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│                            ┌──────────┐                                     │
│              创建实例       │ CREATED  │                                     │
│              ──────────────►│ (已创建) │                                     │
│                            └────┬─────┘                                     │
│                                 │                                           │
│                                 │ 用户扫码                                   │
│                                 ▼                                           │
│                            ┌──────────┐                                     │
│                      ┌─────│ CONNECTED│◄────────┐                          │
│             有用户在线 │    │ (已连接) │        │ 重连成功                   │
│                      │    └────┬─────┘        │                            │
│                      │         │              │                            │
│                      │         │ 连接断开     │                            │
│                      │         ▼              │                            │
│                      │    ┌──────────┐        │                            │
│                      │    │DISCONNECT│────────┘                            │
│                      │    │ (已断开) │                                     │
│                      │    └────┬─────┘                                     │
│                      │         │                                           │
│                      │         │ /reset-qr                                 │
│                      │         ▼                                           │
│                      │    ┌──────────┐                                     │
│                      └───►│  RESET   │                                     │
│         所有用户离线       │ (已重置) │                                     │
│                           └──────────┘                                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 4. 错误处理

### 4.1 错误消息模板

| 错误类型 | 消息 | 动作建议 |
|----------|------|----------|
| 未绑定 | "⚠️ 请先扫描二维码绑定实例" | [如何绑定?] |
| 实例不存在 | "❌ 实例 `xxx` 不存在" | [查看已绑定实例] |
| 连接失败 | "🔴 无法连接到 OpenCode" | [检查状态] [重试] |
| 二维码无效 | "❌ 二维码无效或已过期" | [获取新二维码] |
| 权限不足 | "🚫 你没有该实例的访问权限" | [联系管理员] |
| 网络超时 | "⏱️ 请求超时，请稍后重试" | [重试] |
| 系统错误 | "💥 系统错误，请稍后重试" | [查看日志] [报告问题] |

### 4.2 错误恢复流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           错误恢复流程                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   检测到错误                                                                │
│       │                                                                     │
│       ▼                                                                     │
│   ┌───────────────┐                                                        │
│   │ 是否可重试?   │                                                        │
│   └───────┬───────┘                                                        │
│           │                                                                 │
│     Yes   │   No                                                           │
│     ┌─────┴─────┐                                                          │
│     ▼           ▼                                                          │
│ ┌───────┐  ┌────────────┐                                                  │
│ │ 重试  │  │ 显示错误   │                                                  │
│ │ 3次   │  │ + 解决方案 │                                                  │
│ └───┬───┘  └────────────┘                                                  │
│     │                                                                       │
│     ▼                                                                       │
│ ┌───────────┐                                                              │
│ │ 仍然失败? │                                                              │
│ └─────┬─────┘                                                              │
│       │                                                                     │
│   Yes │   No                                                               │
│   ┌───┴───┐                                                                │
│   ▼       ▼                                                                │
│ 记录日志  继续                                                             │
│ 通知用户                                                                   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 5. 多语言支持

### 5.1 语言检测逻辑

```python
def detect_language(user_id: str, message: Message) -> str:
    # 1. 用户设置优先
    if user_lang := get_user_preference(user_id):
        return user_lang
    
    # 2. Telegram 语言代码
    if message.from_user and message.from_user.language_code:
        lang = message.from_user.language_code
        if lang.startswith("zh"):
            return "zh"
        return "en"
    
    # 3. 默认英文
    return "en"
```

### 5.2 消息模板 (中文)

```yaml
messages:
  welcome:
    title: "🎉 绑定成功！"
    body: |
      实例: `{instance_name}`
      ID: `{instance_id}`
      
      📡 在线用户: {online_users}
    footer: "💡 发送任何消息开始与 AI 对话"
  
  status:
    title: "📦 实例状态"
    instance_line: "▸ {name} {active_marker}"
    session_count: "会话: {count} 个活跃"
    online: "🟢 在线"
    offline: "🔴 离线"
  
  errors:
    not_bound: "⚠️ 请先扫描二维码绑定实例"
    instance_not_found: "❌ 实例 `{name}` 不存在"
    connection_failed: "🔴 无法连接到 OpenCode"
    qr_invalid: "❌ 二维码无效或已过期"
```

### 5.3 消息模板 (英文)

```yaml
messages:
  welcome:
    title: "🎉 Bound Successfully!"
    body: |
      Instance: `{instance_name}`
      ID: `{instance_id}`
      
      📡 Online users: {online_users}
    footer: "💡 Send any message to start chatting with AI"
  
  status:
    title: "📦 Instance Status"
    instance_line: "▸ {name} {active_marker}"
    session_count: "Sessions: {count} active"
    online: "🟢 Online"
    offline: "🔴 Offline"
  
  errors:
    not_bound: "⚠️ Please scan QR code to bind an instance first"
    instance_not_found: "❌ Instance `{name}` not found"
    connection_failed: "🔴 Cannot connect to OpenCode"
    qr_invalid: "❌ QR code invalid or expired"
```

---

## 附录 A: 图标规范

| 用途 | 图标 | 含义 |
|------|------|------|
| 成功 | ✅ | 操作成功 |
| 警告 | ⚠️ | 需要注意 |
| 错误 | ❌ | 操作失败 |
| 处理中 | ⏳ | 正在处理 |
| AI 回复 | 🤖 | 来自 OpenCode |
| 实例 | 📦 | 实例相关 |
| 在线状态 | 📡 | 用户在线 |
| 终端 | 🌐 | Web 访问 |
| 帮助 | 💡 | 提示信息 |
| 系统 | ⚙️ | 系统设置 |

---

## 附录 B: 颜色规范 (钉钉卡片)

| 用途 | 颜色代码 | 示例 |
|------|----------|------|
| 主色调 | #1890FF | 链接、按钮 |
| 成功 | #52C41A | 成功状态 |
| 警告 | #FAAD14 | 警告信息 |
| 错误 | #F5222D | 错误信息 |
| 次要文本 | #8C8C8C | 辅助说明 |
| 背景 | #F5F5F5 | 卡片背景 |

---

*文档生成时间: 2026-01-13*
